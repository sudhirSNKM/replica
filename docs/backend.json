{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an authenticated user account in the system, primarily linking to external authentication (Firebase Auth). Passwords and other sensitive authentication data are managed by Firebase Auth and not stored here.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity, typically the Firebase Authentication User ID (UID)."
        },
        "email": {
          "type": "string",
          "description": "The user's primary email address, obtained from the authentication system.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account's data was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a specific viewing profile associated with a user account, allowing for multiple personalized experiences per user (e.g., 'Kids', 'Dad's Profile').",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User this profile belongs to. (Relationship: User 1:N UserProfile)"
        },
        "name": {
          "type": "string",
          "description": "The display name for the user profile (e.g., 'John's Profile', 'Kids')."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL to the avatar image used for this profile.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "createdAt",
        "updatedAt"
      ]
    },
    "Content": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Content",
      "type": "object",
      "description": "Represents a single piece of media content such as a movie or TV show episode, including all its metadata and media URLs.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Content entity."
        },
        "title": {
          "type": "string",
          "description": "The main title of the content (e.g., 'Replicant Dawn')."
        },
        "description": {
          "type": "string",
          "description": "A brief summary or synopsis of the content."
        },
        "tagline": {
          "type": "string",
          "description": "A short, catchy phrase or slogan for the content, often used in hero sections."
        },
        "thumbnailUrl": {
          "type": "string",
          "description": "URL to the primary poster or thumbnail image for the content, typically hosted in Firebase Storage.",
          "format": "uri"
        },
        "videoUrl": {
          "type": "string",
          "description": "URL to the main video file for streaming the content, typically hosted in Firebase Storage.",
          "format": "uri"
        },
        "trailerUrl": {
          "type": "string",
          "description": "URL to a short, muted video preview or trailer for the content, used for interactive previews.",
          "format": "uri"
        },
        "durationMinutes": {
          "type": "number",
          "description": "The total duration of the content in minutes."
        },
        "releaseDate": {
          "type": "string",
          "description": "The date when the content was originally released.",
          "format": "date"
        },
        "rating": {
          "type": "string",
          "description": "The content rating (e.g., 'PG-13', 'TV-MA', 'G')."
        },
        "imdbRating": {
          "type": "number",
          "description": "The IMDb rating for the content, on a scale of 0 to 10."
        },
        "genreIds": {
          "type": "array",
          "description": "References to the Genres this content belongs to. (Relationship: Content N:N Genre)",
          "items": {
            "type": "string"
          }
        },
        "directorIds": {
          "type": "array",
          "description": "References to the People who directed this content. (Relationship: Content N:N Person)",
          "items": {
            "type": "string"
          }
        },
        "castIds": {
          "type": "array",
          "description": "References to the People who are part of the cast for this content. (Relationship: Content N:N Person)",
          "items": {
            "type": "string"
          }
        },
        "isFeatured": {
          "type": "boolean",
          "description": "A boolean indicating if the content should be prominently featured, for example, in the hero section."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the content metadata was created or uploaded.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the content metadata was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "description",
        "thumbnailUrl",
        "videoUrl",
        "durationMinutes",
        "releaseDate",
        "rating",
        "genreIds",
        "isFeatured",
        "createdAt",
        "updatedAt"
      ]
    },
    "Genre": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Genre",
      "type": "object",
      "description": "Represents a specific classification or type of content (e.g., 'Action', 'Sci-Fi'), used for filtering and organization.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Genre entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the genre (e.g., 'Action', 'Comedy', 'Thriller')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the genre."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the genre was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the genre was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt",
        "updatedAt"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a curated list or browsing section for content (e.g., 'Trending', 'Top Picks', 'New Releases'), distinct from genre classifications.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the category (e.g., 'Trending', 'Top Picks')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the category."
        },
        "contentIds": {
          "type": "array",
          "description": "References to the Content items included in this curated category. (Relationship: Category N:N Content)",
          "items": {
            "type": "string"
          }
        },
        "order": {
          "type": "number",
          "description": "A numerical value to determine the display order of categories in the UI."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the category was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the category was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "contentIds",
        "createdAt",
        "updatedAt"
      ]
    },
    "WatchlistItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WatchlistItem",
      "type": "object",
      "description": "Represents a content item that a specific user profile has added to their personal watchlist for later viewing.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the WatchlistItem entity."
        },
        "userProfileId": {
          "type": "string",
          "description": "Reference to the UserProfile that owns this watchlist item. (Relationship: UserProfile 1:N WatchlistItem)"
        },
        "contentId": {
          "type": "string",
          "description": "Reference to the Content item that is on the watchlist. (Relationship: Content 1:N WatchlistItem)"
        },
        "addedAt": {
          "type": "string",
          "description": "Timestamp indicating when the content was added to the watchlist.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userProfileId",
        "contentId",
        "addedAt"
      ]
    },
    "ContinueWatchingItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ContinueWatchingItem",
      "type": "object",
      "description": "Tracks the playback progress for a specific content item by a user profile, enabling seamless resume playback functionality across devices.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ContinueWatchingItem entity."
        },
        "userProfileId": {
          "type": "string",
          "description": "Reference to the UserProfile whose playback progress is being tracked. (Relationship: UserProfile 1:N ContinueWatchingItem)"
        },
        "contentId": {
          "type": "string",
          "description": "Reference to the Content item being watched. (Relationship: Content 1:N ContinueWatchingItem)"
        },
        "playbackPositionSeconds": {
          "type": "number",
          "description": "The last recorded playback position in seconds for the content, allowing playback to resume from this point."
        },
        "lastWatchedAt": {
          "type": "string",
          "description": "Timestamp indicating when the content was last watched and its progress was updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userProfileId",
        "contentId",
        "playbackPositionSeconds",
        "lastWatchedAt"
      ]
    },
    "Person": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Person",
      "type": "object",
      "description": "Represents an individual involved in content production, such as an actor or director, with their biographical details.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Person entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the person (e.g., 'Tom Cruise', 'Steven Spielberg')."
        },
        "bio": {
          "type": "string",
          "description": "A short biographical description of the person."
        },
        "profileImageUrl": {
          "type": "string",
          "description": "URL to an image or photo of the person.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the person's record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the person's record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores core user account data. The document ID (`userId`) must match the Firebase Authentication UID (`request.auth.uid`). This path provides direct ownership for user-specific data, enabling simple, robust security rules.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, corresponding to their Firebase Authentication UID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/profiles/{userProfileId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual viewing profiles associated with a user. Nested under a user's path, `userId` in the path (and explicitly in the document) ensures authorization independence for profile management. The `userProfileId` is the document ID. Access requires `request.auth.uid == userId`.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the parent user."
            },
            {
              "name": "userProfileId",
              "description": "The unique identifier for a specific user profile."
            }
          ]
        }
      },
      {
        "path": "/content/{contentId}",
        "definition": {
          "entityName": "Content",
          "schema": {
            "$ref": "#/backend/entities/Content"
          },
          "description": "Stores metadata for all movies and TV shows. This is a top-level collection, accessible for public reads and restricted to authenticated administrators for write operations.",
          "params": [
            {
              "name": "contentId",
              "description": "The unique identifier for a piece of content (movie or TV show)."
            }
          ]
        }
      },
      {
        "path": "/genres/{genreId}",
        "definition": {
          "entityName": "Genre",
          "schema": {
            "$ref": "#/backend/entities/Genre"
          },
          "description": "Stores definitions for content genres (e.g., 'Action', 'Sci-Fi'). This is a top-level collection, publicly readable and admin-managed for write operations.",
          "params": [
            {
              "name": "genreId",
              "description": "The unique identifier for a specific content genre."
            }
          ]
        }
      },
      {
        "path": "/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": {
            "$ref": "#/backend/entities/Category"
          },
          "description": "Stores curated lists of content (e.g., 'Trending', 'Top Picks'). This is a top-level collection, publicly readable and admin-managed for write operations. Includes denormalized `contentIds` for efficient retrieval of categorized content.",
          "params": [
            {
              "name": "categoryId",
              "description": "The unique identifier for a specific content category."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/profiles/{userProfileId}/watchlist/{watchlistItemId}",
        "definition": {
          "entityName": "WatchlistItem",
          "schema": {
            "$ref": "#/backend/entities/WatchlistItem"
          },
          "description": "Stores content items added to a specific user profile's watchlist. Nested under the user and profile paths, `userId` and `userProfileId` from the path provide authorization independence. The `watchlistItemId` is the document ID. Includes denormalized 'userProfileId' and 'contentId' fields for explicit linking.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the parent user."
            },
            {
              "name": "userProfileId",
              "description": "The unique identifier of the parent user profile."
            },
            {
              "name": "watchlistItemId",
              "description": "The unique identifier for a watchlist item."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/profiles/{userProfileId}/continueWatching/{continueWatchingItemId}",
        "definition": {
          "entityName": "ContinueWatchingItem",
          "schema": {
            "$ref": "#/backend/entities/ContinueWatchingItem"
          },
          "description": "Tracks playback progress for content watched by a specific user profile. Nested under the user and profile paths, `userId` and `userProfileId` from the path provide authorization independence. The `continueWatchingItemId` is the document ID. Includes denormalized 'userProfileId' and 'contentId' fields for explicit linking.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the parent user."
            },
            {
              "name": "userProfileId",
              "description": "The unique identifier of the parent user profile."
            },
            {
              "name": "continueWatchingItemId",
              "description": "The unique identifier for a continue watching item."
            }
          ]
        }
      },
      {
        "path": "/people/{personId}",
        "definition": {
          "entityName": "Person",
          "schema": {
            "$ref": "#/backend/entities/Person"
          },
          "description": "Stores biographical details for individuals involved in content production (e.g., actors, directors). This is a top-level collection, publicly readable and admin-managed for write operations.",
          "params": [
            {
              "name": "personId",
              "description": "The unique identifier for a person (e.g., actor, director)."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "entityName": "AdminRole",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores documents representing users with administrative privileges. The `adminId` (document ID) matches the Firebase Authentication UID. Used for Database Access Control (DBAC) to grant write access to content and configuration. Authorization relies solely on the existence of the document for `request.auth.uid`.",
          "params": [
            {
              "name": "adminId",
              "description": "The unique identifier (UID) of an administrator user."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for 'Replica' prioritizes security, scalability, and debuggability by strictly adhering to the mandated design principles, especially Authorization Independence and Structural Segregation. \n\n**Authorization Independence:**\n1.  **User-Owned Data Denormalization:** For entities like `UserProfile`, `WatchlistItem`, and `ContinueWatchingItem`, the `userId` and `userProfileId` are explicitly included in their hierarchical paths (e.g., `/users/{userId}/profiles/{userProfileId}/watchlist/{watchlistItemId}`). This structure makes the authorization context directly available within the document's path. Consequently, security rules do not need to perform `get()` operations on parent documents (like `UserProfile` or `User`) to verify ownership. This eliminates hierarchical authorization dependencies, enabling atomic operations and simplifying rule logic significantly. For instance, a `WatchlistItem` can be created or updated without needing to first fetch its associated `UserProfile` to check if `request.auth.uid` matches the profile's owner. The rule simply checks if `request.auth.uid == userId` from the path. \n2.  **Admin Roles:** A dedicated top-level collection `/roles_admin/{adminId}` is used. Authorization for admin actions (e.g., creating/updating `Content` or `Genre`) is determined by checking for the existence of `request.auth.uid` within this collection. This avoids `get()` calls on arbitrary documents and offers a clear, scalable DBAC (Database Access Control) mechanism.\n\n**QAPs (Rules are not Filters) Support:**\n1.  **Structural Segregation:** Publicly accessible, globally managed content (`Content`, `Genre`, `Category`, `Person`) is placed in top-level collections (e.g., `/content/{contentId}`, `/genres/{genreId}`). This allows for simple security rules that grant blanket read access (`allow read: if true;`) while restricting write access to authenticated administrators. This design inherently supports QAPs for `list` operations, as all documents within these collections share the same security posture and are intended to be publicly discoverable without complex server-side filtering. \n2.  **Path-Based Ownership for Private Data:** User-specific and profile-specific data (`User`, `UserProfile`, `WatchlistItem`, `ContinueWatchingItem`) are deeply nested under `/users/{userId}` and then `/profiles/{userProfileId}`. This ensures that any `list` operation performed on these collections (e.g., `/users/{userId}/profiles/{userProfileId}/watchlist`) is automatically scoped to the authenticated user and their specific profile. The `userId` and `userProfileId` wildcards in the path directly correspond to `request.auth.uid` and the selected profile, making `list` operations inherently secure and performant without needing complex rule filters or `get()` calls. This structure allows rules like `allow list: if request.auth.uid == userId;` to effectively restrict results to the requesting user's data."
  }
}